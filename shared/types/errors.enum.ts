// TODO it's just a simple & dummy enum
export enum ErrorsEnum {
    CorruptedData = 'wrong_data',
    NotFoundData = 'not_found_data',
    WrongRequest = 'wrong_request',
    GenericError = 'generic_error',
    DBError = 'db_error',
}
